{
  "name": "contextualizer",
  "version": "2.0.1",
  "description": "LLM context window simulator — visualize and simulate how AI context windows fill up",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
